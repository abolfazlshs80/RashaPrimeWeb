@model RashaPrimeWeb.Application.CQRS.Menu.Commands.CreateMenu.CreateMenuCommand

@{
    ViewData["Title"] = "CreateMenu";
}

<h1>CreateMenu</h1>

<h4>CreateMenuCommand</h4>
<hr />
<div class="row">
    <div class="col-md-4">

     
        <form asp-action="CreateMenu">

            <div asp-validation-summary="All" class="text-danger"></div>
	        <div class="form-group">
		        <label asp-for="Title" class="control-label"></label>
                <input asp-for="Title" class="form-control" />
		        @*    @Html.TextBoxFor(a => a.Name, new { @class = "form-control text-danger" }) *@
                <span asp-validation-for="Title" class="text-danger"></span>
	        </div>
	        <div class="form-group">
		        <label asp-for="Title" class="control-label"></label>
		        <input asp-for="Title" class="form-control" />
		        @*    @Html.TextBoxFor(a => a.Name, new { @class = "form-control text-danger" }) *@
		        <span asp-validation-for="Title" class="text-danger"></span>
	        </div>
	      
	        <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-controller="Home">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
